p Welcome Driver #{user.username} with role #{user.role}!

h2 Create a New ESL
form(name="input",action="/eslocators",method="post")
    | *ESL:
    input(type="text",name="esl")
    br
    input(type="submit",value="Create ESL")

if error != ""
    p1= error
